//! Aync and Await

//?async / await: 
//This makes asynchronous code simpler and more readable.By adding the async keyword before a function, you can then use the await keyword inside it, which pauses the execution until the promise is resolved, making the code look more like synchronous code.

//?Promise.all:
//This method allows you to handle multiple promises at once.It returns an array of results when all the promises are resolved.If any promise is rejected, then Promise.all immediately rejects with that erro