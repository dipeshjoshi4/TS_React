

// useMemo use for performance better of application

// useMemo memoize function ke result ko store karta hai or useCllbak function store karke memoized function return karta hai

//useMemo hhok return when depedency change

//?in react always remember first time component load and render its okey . but always make sure second component render when only something change
//ex - if parent render child does not have to render

